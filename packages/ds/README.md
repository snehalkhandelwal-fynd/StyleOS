# @styleos/ds

StyleOS design system. Tokens + React Native components.

```ts
import { colors, spacing, radii, typography, Button, Card } from '@styleos/ds';
```

For the package contract, layout, and contribution rules see [`docs/claude/ds-package.md`](../../docs/claude/ds-package.md).
For what consumers can use (tokens + components catalog), see the docs under [`docs/claude/`](../../docs/claude/) — `tokens-*.md`, `ds-components.md`, `icons.md`.
